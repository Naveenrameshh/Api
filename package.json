{
    "name": "json-server",
    "version": "1.0.0",
    "main": "server.js",
    "scripts": {
  "start": "chmod +x ./node_modules/.bin/json-server && json-server --watch db.json --port $PORT --host 0.0.0.0"
    },
    "dependencies": {
        "json-server": "^0.17.4"
    }
}
